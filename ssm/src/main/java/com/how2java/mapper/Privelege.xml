<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.how2java.mapper.PrivelegeMapper">
    <insert id="add" parameterType="Privelege" >
	        insert into privelege ( id, name, description ) values ( #{id}, #{name}, #{description} )
	    </insert>

    <delete id="delete" parameterType="Privelege" >
	        delete from privelege where id= #{id}
	    </delete>

    <select id="get" parameterType="String" resultType="Privelege">
	        select * from   privelege  where id= #{id}
	    </select>

    <update id="update" parameterType="Privelege" >
	        update privelege set name=#{name},description=#{description} where id=#{id}
	    </update>
    <select id="list" resultType="Privelege">
	        select * from   privelege
	    </select>
</mapper>

