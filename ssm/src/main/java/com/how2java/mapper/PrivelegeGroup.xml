<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.how2java.mapper.PrivelegeGroupMapper">
    <insert id="add" parameterType="PrivelegeGroup" >
	        insert into pri_group ( id, pri_id, group_id ) values ( #{id}, #{pri_id}, #{group_id} )
	    </insert>

    <delete id="delete" parameterType="PrivelegeGroup" >
	        delete from pri_group where id= #{id}
	    </delete>

    <select id="get" parameterType="String" resultType="PrivelegeGroup">
	        select * from   pri_group  where id= #{id}
	    </select>

    <update id="update" parameterType="PrivelegeGroup" >
	        update pri_group set pri_id=#{pri_id},group_id=#{group_id} where id=#{id}
	    </update>
    <select id="list" resultType="PrivelegeGroup">
	        select * from   pri_group
	    </select>
</mapper>

